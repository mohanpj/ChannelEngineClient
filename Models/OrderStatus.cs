namespace Models
{
    public enum OrderStatus
    {
        IN_PROGRESS,
        SHIPPED,
        IN_BACKORDER,
        MANCO,
        CANCELLED,
        IN_COMBI,
        CLOSED,
        NEW,
        RETURNED,
        REQUIRES_CORRECTION
    }
}